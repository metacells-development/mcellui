---
phase: 03-feedback-components
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/registry/ui/alert-dialog.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "AlertDialog buttonText uses typography tokens instead of hardcoded values"
  artifacts:
    - path: "packages/registry/ui/alert-dialog.tsx"
      provides: "Token-based buttonText styling"
      contains: "overlayTypography.description.fontSize"
  key_links:
    - from: "packages/registry/ui/alert-dialog.tsx"
      to: "overlayTypography"
      via: "import from @metacells/mcellui-core"
      pattern: "overlayTypography\\.description\\.fontSize"
---

<objective>
Fix hardcoded fontSize in AlertDialog buttonText style to use typography tokens.

Purpose: Close the single gap found in Phase 3 verification - the buttonText style has `fontSize: 14` hardcoded instead of using `overlayTypography.description.fontSize`. This violates VISUAL-04 (all components use typography tokens).
Output: AlertDialog fully migrated to token system with no hardcoded typography values.
</objective>

<execution_context>
@/Users/johanneschristler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johanneschristler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-feedback-components/03-VERIFICATION.md
@packages/registry/ui/alert-dialog.tsx
@packages/core/src/theme/components.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded buttonText typography with tokens</name>
  <files>packages/registry/ui/alert-dialog.tsx</files>
  <action>
In the `styles` StyleSheet at line 354-357, update the `buttonText` style:

Current (line 354-357):
```tsx
buttonText: {
  fontSize: 14,
  fontWeight: '600',
},
```

Replace with:
```tsx
buttonText: {
  fontSize: overlayTypography.description.fontSize,
  fontWeight: fontWeight.semibold,
},
```

The `overlayTypography` import already exists (line 49). Add `fontWeight` to the existing imports from `@metacells/mcellui-core` if not already present. Check line 49 for the current imports and add fontWeight alongside overlayTypography if needed.

This change:
1. Uses `overlayTypography.description.fontSize` (14px) - same visual result but token-based
2. Uses `fontWeight.semibold` ('600') - same visual result but token-based
  </action>
  <verify>
1. Run `grep -n "fontSize:" packages/registry/ui/alert-dialog.tsx` - should show only token references, no raw numbers
2. Run `grep "overlayTypography.description.fontSize" packages/registry/ui/alert-dialog.tsx` - should match buttonText style
3. Run `npx tsc --noEmit -p packages/registry` - TypeScript compiles without errors
  </verify>
  <done>AlertDialog buttonText style uses overlayTypography.description.fontSize and fontWeight.semibold tokens with no hardcoded values remaining</done>
</task>

</tasks>

<verification>
Run verification checks:
1. `grep -n "fontSize: [0-9]" packages/registry/ui/alert-dialog.tsx` returns no matches (no hardcoded font sizes)
2. `grep "overlayTypography.description.fontSize" packages/registry/ui/alert-dialog.tsx` returns match in buttonText style
3. TypeScript compilation succeeds
4. Demo app builds and AlertDialog renders correctly (visual spot check)
</verification>

<success_criteria>
- AlertDialog has zero hardcoded typography values
- buttonText style uses overlayTypography.description.fontSize
- fontWeight uses fontWeight.semibold token
- TypeScript compilation passes
- Phase 3 verification would now pass 5/5 must-haves
</success_criteria>

<output>
After completion, create `.planning/phases/03-feedback-components/03-06-SUMMARY.md`
</output>

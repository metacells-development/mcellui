---
phase: 05-data-display
plan: 05
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - apps/demo/app/components/chip-demo.tsx
  - apps/demo/app/components/label-demo.tsx
  - apps/demo/app/components/rating-demo.tsx
  - apps/demo/app/components/avatar-stack-demo.tsx
  - apps/demo/app/components/image-demo.tsx
  - apps/demo/app/components/typography-demo.tsx
autonomous: true

must_haves:
  truths:
    - "All demos use useTheme() for colors and spacing instead of hardcoded values"
    - "Demos show all size variants for each component"
    - "Demos show relevant states (selected, disabled, readonly, error)"
    - "Demo structure follows Sizes -> Features -> States pattern"
  artifacts:
    - path: "apps/demo/app/components/chip-demo.tsx"
      provides: "Comprehensive Chip demo with sizes, variants, states"
      contains: "useTheme"
    - path: "apps/demo/app/components/rating-demo.tsx"
      provides: "Comprehensive Rating demo with sizes, modes"
      contains: "useTheme"
  key_links:
    - from: "apps/demo/app/components/*-demo.tsx"
      to: "useTheme()"
      via: "theme tokens for styling"
      pattern: "const.*colors.*spacing.*=.*useTheme"
---

<objective>
Enhance demos for all data display components with comprehensive coverage.

Purpose: Ensure each demo showcases all variants, sizes, and states using theme tokens for styling, following the established demo structure pattern.

Output: Six enhanced demo files with complete component coverage.
</objective>

<execution_context>
@/Users/johanneschristler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johanneschristler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-data-display/05-RESEARCH.md
@.planning/phases/01-form-inputs/01-05-SUMMARY.md

@apps/demo/app/components/chip-demo.tsx
@apps/demo/app/components/label-demo.tsx
@apps/demo/app/components/rating-demo.tsx
@apps/demo/app/components/avatar-stack-demo.tsx
@apps/demo/app/components/image-demo.tsx
@apps/demo/app/components/typography-demo.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Chip demo</name>
  <files>apps/demo/app/components/chip-demo.tsx</files>
  <action>
Ensure the Chip demo includes comprehensive coverage:

1. Use useTheme() for all styling:
```typescript
const { colors, spacing } = useTheme();
```

2. Section structure (following established pattern):
- **Sizes Section**: Show sm/md/lg chips side by side
- **Variants Section**: Show outline vs filled variants
- **States Section**: selected, unselected, disabled
- **Features Section**: with icon, dismissible (with onRemove)
- **ChipGroup Section**: Multiple chips in a group with wrap

3. Replace any hardcoded colors/spacing:
- `gap: 16` -> `gap: spacing[4]`
- `color: '#...'` -> `color: colors.foreground`
- `marginBottom: 24` -> `marginBottom: spacing[6]`

4. Include interactive states:
- Selected chip (selected={true})
- Disabled chip (disabled={true})
- Dismissible chip (onRemove handler)

Use the Section component pattern from Phase 1 demos if available, otherwise use consistent View wrappers with theme spacing.
  </action>
  <verify>
Check for useTheme usage: `grep "useTheme" apps/demo/app/components/chip-demo.tsx`
Check for hardcoded values: `grep -E "#[0-9a-fA-F]{3,6}|gap: [0-9]+[^%]|margin.*: [0-9]+" apps/demo/app/components/chip-demo.tsx` - should find minimal or none.
  </verify>
  <done>
Chip demo uses theme tokens for all styling, shows sm/md/lg sizes, outline/filled variants, selected/disabled states, icons, and dismissible chips.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance Rating and AvatarStack demos</name>
  <files>apps/demo/app/components/rating-demo.tsx, apps/demo/app/components/avatar-stack-demo.tsx</files>
  <action>
**Rating demo:**
1. Use useTheme() for styling
2. Sections:
- **Sizes Section**: sm/md/lg ratings displayed
- **Modes Section**: Interactive vs readonly
- **Precision Section**: Full star vs half-star precision
- **Custom Colors Section**: Different activeColor/inactiveColor
- **Interactive Section**: Stateful rating with value display

3. Interactive example:
```typescript
const [rating, setRating] = useState(3);
<Rating value={rating} onChange={setRating} />
<Text>Current: {rating}/5</Text>
```

**AvatarStack demo:**
1. Use useTheme() for styling
2. Sections:
- **Sizes Section**: sm/md/lg/xl stacks
- **Max Count Section**: Different max values (2, 3, 4, 5)
- **Overflow Section**: Stack with more avatars than max to show +N indicator
- **Fallback Section**: Mix of image avatars and name-only (initials) avatars
- **Overlap Section**: Different overlap values (0.2, 0.3, 0.4)

3. Sample avatar data:
```typescript
const avatars = [
  { source: { uri: '...' }, name: 'Alice' },
  { name: 'Bob' }, // fallback initials
  { source: { uri: '...' }, name: 'Charlie' },
];
```
  </action>
  <verify>
Check for useTheme: `grep "useTheme" apps/demo/app/components/rating-demo.tsx apps/demo/app/components/avatar-stack-demo.tsx`
Check sections exist: `grep -E "Sizes|Modes|Interactive|Overflow" apps/demo/app/components/rating-demo.tsx`
  </verify>
  <done>
Rating demo shows sizes, modes (interactive/readonly), precision options, custom colors. AvatarStack demo shows sizes, max counts, overflow indicator, fallback initials.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enhance Label, Image, and Typography demos</name>
  <files>apps/demo/app/components/label-demo.tsx, apps/demo/app/components/image-demo.tsx, apps/demo/app/components/typography-demo.tsx</files>
  <action>
**Label demo:**
1. Use useTheme() for styling
2. Sections:
- **Sizes Section**: sm/md/lg labels
- **States Section**: default, required, disabled, error
- **Combinations**: required + disabled, error + required

**Image demo:**
1. Use useTheme() for styling
2. Sections:
- **Basic Section**: Simple image with source
- **Loading Section**: Show skeleton loading state (use slow-loading URL or delay)
- **Error Section**: Use invalid URL to show fallback
- **Placeholder Section**: With blur-up placeholder
- **Border Radius Section**: Default radius vs custom radius

3. Include various aspect ratios:
```typescript
<Image source={...} style={{ width: 200, height: 200 }} />
<Image source={...} style={{ width: '100%', aspectRatio: 16/9 }} />
```

**Typography demo:**
1. Use useTheme() for styling
2. Sections:
- **Headings Section**: h1 through h6 (including new h5, h6)
- **Body Section**: bodyLg, body, bodySm
- **UI Text Section**: label, labelSm, button variants
- **Decorations Section**: bold, italic, underline, strikethrough
- **Colors Section**: default, muted, primary, error colors
- **Alignment Section**: left, center, right

Ensure h5 and h6 are displayed to verify the new typography tokens work.
  </action>
  <verify>
Check for useTheme: `grep "useTheme" apps/demo/app/components/label-demo.tsx apps/demo/app/components/image-demo.tsx apps/demo/app/components/typography-demo.tsx`
Check h5/h6 usage: `grep -E 'variant="h5"|variant="h6"' apps/demo/app/components/typography-demo.tsx`
  </verify>
  <done>
Label demo shows sizes and states. Image demo shows loading, error, placeholder. Typography demo shows all variants including h5/h6, colors, and decorations.
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. All six demo files use useTheme() for colors and spacing
2. No hardcoded hex colors in demo StyleSheets
3. Each demo has clear section organization
4. Typography demo includes h5 and h6 examples
5. Rating demo has interactive state management
6. Demo app builds without errors: `cd apps/demo && npx expo export --platform web`
</verification>

<success_criteria>
- All demos use `const { colors, spacing } = useTheme()` pattern
- Chip demo: sizes, variants (outline/filled), states (selected/disabled), icons, dismissible
- Rating demo: sizes, modes (interactive/readonly), precision (full/half), custom colors
- AvatarStack demo: sizes, max counts, overflow, fallback initials, overlap variations
- Label demo: sizes, states (required, disabled, error)
- Image demo: loading skeleton, error fallback, placeholder blur, border radius
- Typography demo: all headings (h1-h6), body variants, decorations, colors
- No hardcoded colors or spacing values in StyleSheets
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-display/05-05-SUMMARY.md`
</output>

---
phase: 05-data-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/theme/components.ts
  - packages/core/src/theme/radius.ts
autonomous: true

must_haves:
  truths:
    - "All data display component tokens are centralized in components.ts"
    - "componentRadius has entries for chip and image"
    - "Tokens use existing spacing/fontSize/fontWeight references"
  artifacts:
    - path: "packages/core/src/theme/components.ts"
      provides: "chipTokens, labelTokens, ratingTokens, avatarStackTokens"
      contains: "export const chipTokens"
    - path: "packages/core/src/theme/radius.ts"
      provides: "componentRadius.chip, componentRadius.image"
      contains: "chip:"
  key_links:
    - from: "packages/core/src/theme/components.ts"
      to: "typography.ts, spacing.ts"
      via: "import"
      pattern: "import.*fontSize.*fontWeight"
---

<objective>
Extend the core token system with data display component tokens.

Purpose: Establish the token foundation for Chip, Label, Rating, and AvatarStack so they can reference centralized values instead of inline SIZE_CONFIG objects.

Output: Extended components.ts with new token objects and radius.ts with new componentRadius entries.
</objective>

<execution_context>
@/Users/johanneschristler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johanneschristler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-data-display/05-RESEARCH.md

@packages/core/src/theme/components.ts
@packages/core/src/theme/radius.ts
@packages/core/src/theme/typography.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data display component tokens to components.ts</name>
  <files>packages/core/src/theme/components.ts</files>
  <action>
Add new token objects for data display components after the existing avatarTokens section:

1. chipTokens - migrate from Chip's inline SIZE_CONFIG:
```typescript
export const chipTokens = {
  sm: {
    paddingHorizontal: spacing[2.5],  // was 10
    paddingVertical: spacing[1],       // was 4
    fontSize: fontSize.sm,             // was 12
    iconSize: iconSize.sm,             // was 14
  },
  md: {
    paddingHorizontal: spacing[3.5],  // was 14
    paddingVertical: spacing[1.5],     // was 6
    fontSize: fontSize.base,           // was 14
    iconSize: iconSize.sm,             // was 16 -> use sm for consistency
  },
  lg: {
    paddingHorizontal: spacing[4.5],  // was 18
    paddingVertical: spacing[2],       // was 8
    fontSize: fontSize.md,             // was 16
    iconSize: iconSize.md,             // was 18
  },
} as const;
```

2. labelTokens - migrate from Label's inline sizeTokens:
```typescript
export const labelTokens = {
  sm: {
    fontSize: fontSize.sm,             // was 12
    lineHeight: 16,
    fontWeight: fontWeight.medium,
  },
  md: {
    fontSize: fontSize.base,           // was 14
    lineHeight: 20,
    fontWeight: fontWeight.medium,
  },
  lg: {
    fontSize: fontSize.md,             // was 16
    lineHeight: 24,
    fontWeight: fontWeight.medium,
  },
} as const;
```

3. ratingTokens - migrate from Rating's inline SIZE_CONFIG:
```typescript
export const ratingTokens = {
  sm: {
    starSize: 18,
    gap: spacing[0.5],                 // was 2
  },
  md: {
    starSize: 26,
    gap: spacing[1],                   // was 4
  },
  lg: {
    starSize: 34,
    gap: spacing[1.5],                 // was 6
  },
} as const;
```

4. avatarStackTokens - migrate from AvatarStack's inline SIZE_CONFIG:
```typescript
export const avatarStackTokens = {
  sm: {
    size: 28,
    fontSize: fontSize['2xs'],         // was 10
    borderWidth: 2,
  },
  md: {
    size: 36,
    fontSize: fontSize.sm,             // was 12
    borderWidth: 2,
  },
  lg: {
    size: 44,
    fontSize: fontSize.base,           // was 14
    borderWidth: 3,
  },
  xl: {
    size: 56,
    fontSize: fontSize.lg,             // was 18
    borderWidth: 3,
  },
} as const;
```

Then add all four to the `components` export object at the bottom:
```typescript
export const components = {
  // ... existing entries ...
  chip: chipTokens,
  label: labelTokens,
  rating: ratingTokens,
  avatarStack: avatarStackTokens,
  // ... rest ...
} as const;
```
  </action>
  <verify>
Run `npx tsc --noEmit -p packages/core` - no type errors.
Grep for new exports: `grep -E "chipTokens|labelTokens|ratingTokens|avatarStackTokens" packages/core/src/theme/components.ts`
  </verify>
  <done>
Four new token objects (chipTokens, labelTokens, ratingTokens, avatarStackTokens) exist in components.ts and are included in the components export.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add componentRadius entries for chip and image</name>
  <files>packages/core/src/theme/radius.ts</files>
  <action>
Add new entries to ComponentRadiusTokens interface and createComponentRadius function:

1. Update ComponentRadiusTokens interface to add:
```typescript
chip: number;
image: number;
```

2. Update createComponentRadius function to add:
```typescript
// Chip: uses lg radius for pill-like but not full circular
chip: radiusTokens.lg,
// Image: uses md radius for default image corners
image: radiusTokens.md,
```

Place these with the other entries in alphabetical order or logical grouping (after checkbox, near card).
  </action>
  <verify>
Run `npx tsc --noEmit -p packages/core` - no type errors.
Check entries: `grep -E "chip:|image:" packages/core/src/theme/radius.ts`
  </verify>
  <done>
componentRadius.chip and componentRadius.image entries exist in radius.ts interface and createComponentRadius function.
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. `npx tsc --noEmit -p packages/core` passes without errors
2. chipTokens, labelTokens, ratingTokens, avatarStackTokens exist in components.ts
3. All token objects are included in the components export
4. componentRadius.chip and componentRadius.image exist in radius.ts
</verification>

<success_criteria>
- chipTokens with sm/md/lg sizes for padding, fontSize, iconSize
- labelTokens with sm/md/lg sizes for fontSize, lineHeight, fontWeight
- ratingTokens with sm/md/lg sizes for starSize and gap
- avatarStackTokens with sm/md/lg/xl sizes for size, fontSize, borderWidth
- componentRadius.chip = radiusTokens.lg
- componentRadius.image = radiusTokens.md
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-display/05-01-SUMMARY.md`
</output>

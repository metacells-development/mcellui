---
phase: 04-progress-loading
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - apps/demo/components/demos/spinner-demo.tsx
  - apps/demo/components/demos/skeleton-demo.tsx
  - apps/demo/components/demos/progress-demo.tsx
  - apps/demo/components/demos/circular-progress-demo.tsx
  - apps/demo/components/demos/pull-to-refresh-demo.tsx
autonomous: true

must_haves:
  truths:
    - "All demos show size variants (sm/md/lg) side by side"
    - "All demos show color variants where applicable"
    - "Progress demos show determinate and indeterminate states"
    - "Demos include interactive examples with state changes"
    - "Demos include realistic use case examples"
  artifacts:
    - path: "apps/demo/components/demos/spinner-demo.tsx"
      provides: "Comprehensive Spinner demo"
      min_lines: 60
    - path: "apps/demo/components/demos/skeleton-demo.tsx"
      provides: "Comprehensive Skeleton demo"
      min_lines: 100
    - path: "apps/demo/components/demos/progress-demo.tsx"
      provides: "Comprehensive Progress demo"
      min_lines: 120
    - path: "apps/demo/components/demos/circular-progress-demo.tsx"
      provides: "Comprehensive CircularProgress demo"
      min_lines: 180
    - path: "apps/demo/components/demos/pull-to-refresh-demo.tsx"
      provides: "Comprehensive PullToRefresh demo"
      min_lines: 100
  key_links:
    - from: "apps/demo/components/demos/spinner-demo.tsx"
      to: "@/components/ui/spinner"
      via: "import Spinner"
      pattern: "import.*Spinner.*from"
---

<objective>
Enhance all progress and loading component demos with comprehensive coverage of sizes, colors, states, and use cases.

Purpose: Provide complete visual documentation of all component variants and interaction patterns, following the established demo patterns from Phase 1-3.

Output: Enhanced demo files with consistent section structure and comprehensive variant coverage
</objective>

<execution_context>
@/Users/johanneschristler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johanneschristler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-progress-loading/04-RESEARCH.md
@apps/demo/components/demos/spinner-demo.tsx
@apps/demo/components/demos/skeleton-demo.tsx
@apps/demo/components/demos/progress-demo.tsx
@apps/demo/components/demos/circular-progress-demo.tsx
@apps/demo/components/demos/pull-to-refresh-demo.tsx
@apps/demo/components/demos/section.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Spinner demo with use cases section</name>
  <files>apps/demo/components/demos/spinner-demo.tsx</files>
  <action>
Enhance the existing Spinner demo to add practical use case examples. Current demo already has Sizes and Colors sections. Add:

1. **Use Cases section** - Show spinner in context:
```typescript
<Section title="Use Cases">
  <View style={styles.useCases}>
    {/* Button loading state */}
    <View style={styles.useCase}>
      <View style={[styles.loadingButton, { backgroundColor: colors.primary, borderRadius: radius.md }]}>
        <Spinner size="sm" color="default" style={{ marginRight: 8 }} />
        <Text style={{ color: colors.primaryForeground, fontWeight: '500' }}>Loading...</Text>
      </View>
      <Text style={[styles.useCaseLabel, { color: colors.foregroundMuted }]}>Button loading</Text>
    </View>

    {/* Inline loading */}
    <View style={styles.useCase}>
      <View style={styles.inlineRow}>
        <Text style={{ color: colors.foreground }}>Saving changes</Text>
        <Spinner size="sm" color="primary" />
      </View>
      <Text style={[styles.useCaseLabel, { color: colors.foregroundMuted }]}>Inline indicator</Text>
    </View>

    {/* Full screen loading placeholder */}
    <View style={styles.useCase}>
      <View style={[styles.loadingCard, { backgroundColor: colors.card, borderRadius: radius.lg }]}>
        <Spinner size="lg" color="muted" />
        <Text style={[styles.loadingText, { color: colors.foregroundMuted }]}>Loading content...</Text>
      </View>
      <Text style={[styles.useCaseLabel, { color: colors.foregroundMuted }]}>Card loading</Text>
    </View>
  </View>
</Section>
```

2. Add styles for use cases:
```typescript
useCases: {
  gap: 20,
},
useCase: {
  alignItems: 'center',
  gap: 8,
},
useCaseLabel: {
  fontSize: 12,
},
loadingButton: {
  flexDirection: 'row',
  alignItems: 'center',
  paddingHorizontal: 16,
  paddingVertical: 10,
},
inlineRow: {
  flexDirection: 'row',
  alignItems: 'center',
  gap: 8,
},
loadingCard: {
  padding: 24,
  alignItems: 'center',
  gap: 12,
  width: 160,
},
loadingText: {
  fontSize: 13,
},
```

3. Add useTheme import and destructure colors, radius:
```typescript
const { colors, radius } = useTheme();
```
  </action>
  <verify>
Demo file compiles without errors.
Demo shows: Sizes, Colors, Large with Colors, Use Cases sections.
Use cases section has button loading, inline indicator, and card loading examples.
  </verify>
  <done>
Spinner demo includes use cases showing spinner in button, inline, and card loading contexts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance Skeleton demo with animation toggle</name>
  <files>apps/demo/components/demos/skeleton-demo.tsx</files>
  <action>
Enhance the existing Skeleton demo. Current demo is already comprehensive but add:

1. **Animation Toggle section** - Show with/without animation:
```typescript
<Section title="Animation Control">
  <View style={styles.animationControl}>
    <View style={styles.animationRow}>
      <Text style={{ color: colors.foreground, flex: 1 }}>With Animation</Text>
      <Skeleton width={100} height={20} animate={true} />
    </View>
    <View style={styles.animationRow}>
      <Text style={{ color: colors.foreground, flex: 1 }}>Static (no shimmer)</Text>
      <Skeleton width={100} height={20} animate={false} />
    </View>
  </View>
</Section>
```

2. **Profile Card Example** (more detailed than current Card Example):
```typescript
<Section title="Profile Card Loading">
  <View style={[styles.profileCard, { backgroundColor: colors.card, borderRadius: radius.lg }]}>
    {/* Cover image placeholder */}
    <Skeleton width="100%" height={80} radius="none" />
    {/* Avatar overlapping cover */}
    <View style={styles.profileAvatarContainer}>
      <Skeleton circle size={64} />
    </View>
    {/* Profile info */}
    <View style={styles.profileInfo}>
      <Skeleton width={140} height={18} />
      <Skeleton width={100} height={14} style={{ marginTop: 6 }} />
      <SkeletonText lines={2} style={{ marginTop: 12 }} />
    </View>
  </View>
</Section>
```

3. Add styles:
```typescript
animationControl: {
  gap: 16,
},
animationRow: {
  flexDirection: 'row',
  alignItems: 'center',
},
profileCard: {
  overflow: 'hidden',
},
profileAvatarContainer: {
  marginTop: -32,
  marginLeft: 16,
},
profileInfo: {
  padding: 16,
  paddingTop: 8,
},
```
  </action>
  <verify>
Demo file compiles without errors.
Demo shows: Basic Shapes, Radius Variants, Circles, Text Lines, Animation Control, Card Example, Profile Card Loading, List Example sections.
  </verify>
  <done>
Skeleton demo includes animation toggle and profile card loading example in addition to existing sections.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enhance Progress and CircularProgress demos with indeterminate state showcase</name>
  <files>apps/demo/components/demos/progress-demo.tsx, apps/demo/components/demos/circular-progress-demo.tsx</files>
  <action>
**Progress demo** - Add indeterminate section with all colors:
```typescript
<Section title="Indeterminate Colors">
  <View style={styles.colors}>
    <View style={styles.colorRow}>
      <Text style={[styles.colorLabel, { color: colors.foregroundMuted }]}>default</Text>
      <Progress indeterminate color="default" />
    </View>
    <View style={styles.colorRow}>
      <Text style={[styles.colorLabel, { color: colors.foregroundMuted }]}>primary</Text>
      <Progress indeterminate color="primary" />
    </View>
    <View style={styles.colorRow}>
      <Text style={[styles.colorLabel, { color: colors.foregroundMuted }]}>success</Text>
      <Progress indeterminate color="success" />
    </View>
  </View>
</Section>
```

Add colorRow and colorLabel styles:
```typescript
colorRow: {
  flexDirection: 'row',
  alignItems: 'center',
  gap: 12,
},
colorLabel: {
  width: 60,
  fontSize: 12,
},
```

**CircularProgress demo** - Add indeterminate section:
```typescript
<Section title="Indeterminate">
  <Card>
    <CardContent style={{ paddingTop: spacing[4] }}>
      <View style={{ flexDirection: 'row', justifyContent: 'space-around', alignItems: 'center' }}>
        <View style={{ alignItems: 'center' }}>
          <CircularProgress indeterminate size="sm" />
          <Text style={[styles.sizeLabel, { color: colors.foregroundMuted, marginTop: spacing[2] }]}>
            Small
          </Text>
        </View>
        <View style={{ alignItems: 'center' }}>
          <CircularProgress indeterminate size="md" />
          <Text style={[styles.sizeLabel, { color: colors.foregroundMuted, marginTop: spacing[2] }]}>
            Medium
          </Text>
        </View>
        <View style={{ alignItems: 'center' }}>
          <CircularProgress indeterminate size="lg" />
          <Text style={[styles.sizeLabel, { color: colors.foregroundMuted, marginTop: spacing[2] }]}>
            Large
          </Text>
        </View>
      </View>
    </CardContent>
  </Card>
</Section>
```
  </action>
  <verify>
Both demo files compile without errors.
Progress demo shows indeterminate variants with colors.
CircularProgress demo shows indeterminate sizes.
  </verify>
  <done>
Progress demo has Indeterminate Colors section. CircularProgress demo has Indeterminate section with all size variants.
  </done>
</task>

</tasks>

<verification>
1. All demo files compile: `cd apps/demo && npx tsc --noEmit`
2. Start demo app and navigate to each component
3. Verify all sections render correctly
4. Verify animations work (shimmer, indeterminate progress)
5. Verify interactive examples respond to user input
</verification>

<success_criteria>
- Spinner demo has Use Cases section with button, inline, card examples
- Skeleton demo has Animation Control and Profile Card Loading sections
- Progress demo has Indeterminate Colors section
- CircularProgress demo has Indeterminate section with size variants
- PullToRefresh demo remains unchanged (already comprehensive)
- All demos use Section component for consistent structure
- All demos use theme tokens for colors and spacing
</success_criteria>

<output>
After completion, create `.planning/phases/04-progress-loading/04-04-SUMMARY.md`
</output>

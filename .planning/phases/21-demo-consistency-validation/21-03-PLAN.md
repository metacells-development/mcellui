---
phase: 21-demo-consistency-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/demo/components/demos/carousel-demo.tsx
  - apps/demo/components/demos/toggle-demo.tsx
  - apps/demo/components/demos/chip-demo.tsx
  - apps/demo/components/demos/segmented-control-demo.tsx
  - apps/demo/components/demos/circular-progress-demo.tsx
  - apps/demo/components/demos/card-demo.tsx
  - apps/demo/components/demos/horizontal-list-demo.tsx
  - apps/demo/components/demos/list-demo.tsx
autonomous: true

must_haves:
  truths:
    - "High-violation demo files use typography tokens"
    - "Demo showcase colors remain hardcoded (intentional examples)"
    - "Demos render correctly in both light and dark mode"
  artifacts:
    - path: "apps/demo/components/demos/carousel-demo.tsx"
      provides: "Carousel examples with theme-adaptive text"
      contains: "fontSize.lg"
    - path: "apps/demo/components/demos/toggle-demo.tsx"
      provides: "Toggle examples with semantic colors"
      contains: "colors.foreground"
  key_links:
    - from: "apps/demo/components/demos/*.tsx"
      to: "useTheme()"
      via: "typography and color token access"
      pattern: "fontSize\\["
---

<objective>
Migrate high-violation demo component files to use theme tokens

Purpose: Demo files show example usage of each component. Files like carousel-demo (14 colors, 2 fontSize), toggle-demo (8 colors, 4 fontSize), and others have mixed violations. Migrating these ensures demos demonstrate proper token usage.

Output: 8 high-violation demo files migrated to use semantic tokens while preserving intentional showcase colors
</objective>

<execution_context>
@/Users/johanneschristler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johanneschristler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-demo-consistency-validation/21-RESEARCH.md
@apps/demo/components/demos/carousel-demo.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate carousel-demo.tsx and toggle-demo.tsx</name>
  <files>
apps/demo/components/demos/carousel-demo.tsx
apps/demo/components/demos/toggle-demo.tsx
  </files>
  <action>
**carousel-demo.tsx:**
This file already uses useTheme() with fontSize tokens for most text. Fix remaining issues:
- `fontSize: 24` (line 47) -> `fontSize: fontSize.xl` (bannerTitleStyle)
- `fontSize: 18` (line 90) -> `fontSize: fontSize.lg` (productPriceStyle)
- `gap: 32` (line 314) -> `gap: spacing[8]`

KEEP HARDCODED (intentional showcase):
- Banner background colors (#FF6B6B, #4ECDC4, etc.) - demonstrating colored carousel items
- Image color array (#E74C3C, #3498DB, etc.) - showing color slides
- Text on colored backgrounds (#fff, rgba(255,255,255,0.85)) - contrast on showcase colors
- Product placeholder colors (#F8F9FA, etc.) - showing product card variations

**toggle-demo.tsx:**
Has 8 hardcoded colors and 4 fontSize values. Analyze which are showcase vs violations:
- UI chrome colors (descriptions, labels) -> migrate to tokens
- Showcase colors (demonstrating toggle states) -> keep hardcoded
- All fontSize values -> migrate to tokens

Update useTheme() destructuring and apply inline styles for dynamic values.
  </action>
  <verify>
grep -E "fontSize:\s*[0-9]+" apps/demo/components/demos/carousel-demo.tsx apps/demo/components/demos/toggle-demo.tsx | grep -v "// showcase" should return 0 matches.
  </verify>
  <done>
carousel-demo.tsx and toggle-demo.tsx use typography tokens. Showcase colors preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate chip-demo.tsx, segmented-control-demo.tsx, circular-progress-demo.tsx</name>
  <files>
apps/demo/components/demos/chip-demo.tsx
apps/demo/components/demos/segmented-control-demo.tsx
apps/demo/components/demos/circular-progress-demo.tsx
  </files>
  <action>
**chip-demo.tsx (7 fontSize, 2 colors):**
- Migrate all fontSize values to tokens
- Check if colors are showcase (chip color examples) or UI chrome
- Apply semantic tokens for UI chrome colors

**segmented-control-demo.tsx (7 fontSize):**
- Migrate all fontSize values to tokens
- These are likely all UI chrome (section titles, labels)

**circular-progress-demo.tsx (8 fontSize):**
- Migrate all fontSize values to tokens
- These are likely UI chrome (percentage displays, labels)

Pattern for all:
```tsx
const { colors, spacing, fontSize, fontWeight } = useTheme();

// Move from StyleSheet to inline for dynamic values
<Text style={[styles.label, { fontSize: fontSize.sm, color: colors.foregroundMuted }]}>
  Label text
</Text>
```
  </action>
  <verify>
grep -E "fontSize:\s*[0-9]+" apps/demo/components/demos/chip-demo.tsx apps/demo/components/demos/segmented-control-demo.tsx apps/demo/components/demos/circular-progress-demo.tsx should return 0 matches.
  </verify>
  <done>
chip-demo.tsx, segmented-control-demo.tsx, and circular-progress-demo.tsx use typography tokens.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate card-demo.tsx, horizontal-list-demo.tsx, list-demo.tsx</name>
  <files>
apps/demo/components/demos/card-demo.tsx
apps/demo/components/demos/horizontal-list-demo.tsx
apps/demo/components/demos/list-demo.tsx
  </files>
  <action>
**card-demo.tsx (7 fontSize):**
- Migrate all fontSize values to typography tokens
- Card examples should use semantic colors for text

**horizontal-list-demo.tsx (7 colors):**
- Analyze which colors are showcase (colored list items) vs UI chrome
- Colored item backgrounds (#3498DB, #E74C3C, etc.) are likely showcase -> keep
- Text colors and labels -> migrate to tokens

**list-demo.tsx (7 colors):**
- Same analysis as horizontal-list-demo
- Item showcase colors -> keep hardcoded
- UI chrome colors -> migrate to tokens

Apply the standard pattern: useTheme() destructuring, inline styles for dynamic tokens.
  </action>
  <verify>
grep -E "fontSize:\s*[0-9]+" apps/demo/components/demos/card-demo.tsx apps/demo/components/demos/horizontal-list-demo.tsx apps/demo/components/demos/list-demo.tsx should return 0 matches.
  </verify>
  <done>
card-demo.tsx, horizontal-list-demo.tsx, and list-demo.tsx use theme tokens appropriately. Showcase colors preserved.
  </done>
</task>

</tasks>

<verification>
1. `grep -rE "fontSize:\s*[0-9]+" apps/demo/components/demos/{carousel,toggle,chip,segmented-control,circular-progress,card,horizontal-list,list}-demo.tsx` should return 0 matches
2. Run demo app and navigate to each component demo
3. Toggle dark mode - demos should render correctly
4. Verify showcase items still show intended color variations
</verification>

<success_criteria>
- All 8 demo files use typography tokens
- Showcase colors remain hardcoded (demonstrating component color options)
- UI chrome uses semantic color tokens
- No visual regressions
- Dark mode works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/21-demo-consistency-validation/21-03-SUMMARY.md`
</output>

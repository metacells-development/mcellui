---
phase: 07-navigation-interaction
plan: 05
type: execute
wave: 3
depends_on: ["07-02", "07-03", "07-04"]
files_modified:
  - apps/demo/components/demos/tabs-demo.tsx
  - apps/demo/components/demos/accordion-demo.tsx
  - apps/demo/components/demos/collapsible-demo.tsx
  - apps/demo/components/demos/carousel-demo.tsx
  - apps/demo/components/demos/swipeable-row-demo.tsx
autonomous: true

must_haves:
  truths:
    - "All navigation demos show component variants"
    - "All navigation demos show interaction states"
    - "Demos use Section component pattern for organization"
    - "All demos show disabled state examples"
  artifacts:
    - path: "apps/demo/components/demos/tabs-demo.tsx"
      provides: "Enhanced Tabs demo with all variants"
      contains: "Section"
    - path: "apps/demo/components/demos/accordion-demo.tsx"
      provides: "Enhanced Accordion demo with all modes"
      contains: "Section"
    - path: "apps/demo/components/demos/collapsible-demo.tsx"
      provides: "Enhanced Collapsible demo"
      contains: "Section"
    - path: "apps/demo/components/demos/carousel-demo.tsx"
      provides: "Enhanced Carousel demo with indicator variants"
      contains: "Section"
    - path: "apps/demo/components/demos/swipeable-row-demo.tsx"
      provides: "Enhanced SwipeableRow demo"
      contains: "Section"
  key_links:
    - from: "apps/demo/components/demos/tabs-demo.tsx"
      to: "packages/registry/ui/tabs.tsx"
      via: "import Tabs"
      pattern: "from.*tabs"
---

<objective>
Enhance all navigation component demos with comprehensive state and variant coverage

Purpose: Ensure demos showcase all component capabilities including variants, sizes, states (disabled, controlled, uncontrolled), and edge cases (empty states, many items, animations).

Output: Updated demo files with comprehensive coverage using Section component pattern
</objective>

<execution_context>
@/Users/johanneschristler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johanneschristler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-navigation-interaction/07-02-SUMMARY.md
@.planning/phases/07-navigation-interaction/07-03-SUMMARY.md
@.planning/phases/07-navigation-interaction/07-04-SUMMARY.md
@apps/demo/components/demos/tabs-demo.tsx
@apps/demo/components/demos/accordion-demo.tsx
@apps/demo/components/demos/collapsible-demo.tsx
@apps/demo/components/demos/carousel-demo.tsx
@apps/demo/components/demos/swipeable-row-demo.tsx
@apps/demo/components/demos/section.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Tabs and Accordion demos</name>
  <files>apps/demo/components/demos/tabs-demo.tsx, apps/demo/components/demos/accordion-demo.tsx</files>
  <action>
Update Tabs demo to use Section component and add missing coverage:

1. Import Section component (located in same demos directory):
```typescript
import { Section } from './section';
```

2. Replace manual section styling with Section component:
```typescript
<Section title="Pill Variant (Default)">
  <Tabs defaultValue="account">
    ...
  </Tabs>
</Section>
```

3. Ensure existing sections are covered:
- Pill Variant (Default) - already exists
- Three Tabs - already exists
- Underline Variant - already exists
- Controlled Tabs - already exists
- With Disabled Tab - already exists

4. Add if missing:
- Many Tabs (scrollable test with 5+ tabs)
- Keyboard Navigation (accessibility note)

Update Accordion demo similarly:

1. Import Section component and wrap existing sections

2. Ensure existing sections are covered:
- Single Mode (Collapsible) - already exists
- Multiple Mode - already exists
- Controlled (Non-Collapsible) - already exists
- FAQ Style - already exists

3. Add if missing:
- With Disabled Item (show disabled AccordionItem)
- Long Content (test height animation with paragraphs)
  </action>
  <verify>
Run TypeScript compilation:
```bash
cd /Users/johanneschristler/Code/metacells/other/mcellui && npx tsc --noEmit -p apps/demo/tsconfig.json
```
Verify no errors. Check that demos import Section and use consistent structure.
  </verify>
  <done>
Tabs and Accordion demos use Section component for organization. All existing sections preserved. Disabled state examples included. Demo files compile without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance Collapsible and Carousel demos</name>
  <files>apps/demo/components/demos/collapsible-demo.tsx, apps/demo/components/demos/carousel-demo.tsx</files>
  <action>
Update Collapsible demo to use Section component:

1. Read current collapsible-demo.tsx content
2. Import Section component
3. Organize into sections:
- Basic Collapsible (uncontrolled)
- Controlled State
- Without Chevron (showChevron={false})
- Disabled State
- With Custom Trigger (asChild pattern)

Update Carousel demo to use Section component:

1. Read current carousel-demo.tsx content
2. Import Section component
3. Organize into sections:
- Basic Carousel (children pattern)
- With Data + RenderItem (parallax support)
- Autoplay
- Indicator Variants (dot vs line)
- Indicator Positions (top vs bottom)
- Controlled via Ref (scrollToIndex demo)
  </action>
  <verify>
Run TypeScript compilation:
```bash
cd /Users/johanneschristler/Code/metacells/other/mcellui && npx tsc --noEmit -p apps/demo/tsconfig.json
```
Verify no errors. Check that demos import Section and show all variants.
  </verify>
  <done>
Collapsible and Carousel demos use Section component for organization. All variants demonstrated (indicator styles, autoplay, controlled). Demo files compile without errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enhance SwipeableRow demo</name>
  <files>apps/demo/components/demos/swipeable-row-demo.tsx</files>
  <action>
Update SwipeableRow demo to use Section component:

1. Import Section component
2. Organize into sections:
- Email List Example (current main demo)
- Right Actions Only
- Left Actions Only
- Full Swipe Disabled
- Custom Action Width
- With Icons in Actions

The current demo is comprehensive for the email use case. Enhance by:
1. Wrapping in Section components for consistency
2. Adding a "Right Actions Only" section showing simpler usage
3. Adding a "Full Swipe Disabled" section showing fullSwipeEnabled={false}

Keep the email list as main example but add simpler examples above it.
  </action>
  <verify>
Run TypeScript compilation:
```bash
cd /Users/johanneschristler/Code/metacells/other/mcellui && npx tsc --noEmit -p apps/demo/tsconfig.json
```
Verify no errors. Check that demo imports Section and shows all configurations.
  </verify>
  <done>
SwipeableRow demo uses Section component for organization. Shows simplified examples (right-only, left-only, full swipe disabled) in addition to full email example. Demo file compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. All 5 demo files import Section component
2. All demos use consistent Section-based organization
3. tabs-demo.tsx shows: pill variant, underline variant, controlled, disabled tab
4. accordion-demo.tsx shows: single mode, multiple mode, controlled, disabled item
5. collapsible-demo.tsx shows: basic, controlled, without chevron, disabled
6. carousel-demo.tsx shows: basic, autoplay, indicator variants, indicator positions
7. swipeable-row-demo.tsx shows: full example, right-only, left-only, full swipe disabled
8. All demos compile without TypeScript errors
9. No hardcoded colors or spacing (use theme tokens)
</verification>

<success_criteria>
- All demos use Section component pattern
- Each component has variant/mode coverage
- Each component shows disabled state
- Demos follow established pattern from Phase 1-3 (Sizes -> Features -> States)
- Demo app builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/07-navigation-interaction/07-05-SUMMARY.md`
</output>

---
phase: 07-navigation-interaction
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - packages/registry/ui/tabs.tsx
autonomous: true

must_haves:
  truths:
    - "Tabs component uses tabsTokens for font sizes and padding"
    - "Tabs indicator uses componentRadius.tabsIndicator"
    - "Tabs container uses componentRadius.tabs for pill variant"
    - "Tab trigger uses token-based typography"
  artifacts:
    - path: "packages/registry/ui/tabs.tsx"
      provides: "Tabs with centralized tokens"
      contains: "tabsTokens"
  key_links:
    - from: "packages/registry/ui/tabs.tsx"
      to: "packages/core/src/theme/components.ts"
      via: "import tabsTokens"
      pattern: "tabsTokens|TABS_CONSTANTS"
---

<objective>
Migrate Tabs component to use centralized token system

Purpose: Replace hardcoded padding, font sizes, and radius values with tabsTokens and componentRadius for theme consistency and easier customization.

Output: Updated tabs.tsx using centralized tokens
</objective>

<execution_context>
@/Users/johanneschristler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johanneschristler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-navigation-interaction/07-01-SUMMARY.md
@packages/registry/ui/tabs.tsx
@packages/core/src/theme/components.ts
@packages/core/src/theme/radius.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Tabs to centralized tokens</name>
  <files>packages/registry/ui/tabs.tsx</files>
  <action>
Update Tabs component to use centralized tokens. Key changes:

1. Import tokens at top of file:
```typescript
import {
  useTheme,
  areAnimationsDisabled,
  tabsTokens,
  TABS_CONSTANTS,
} from '@metacells/mcellui-core';
```

2. In TabsList component, replace hardcoded values with tokens:
- Replace `padding: 4` with `padding: TABS_CONSTANTS.pillPadding`
- Use `componentRadius.tabs` for container radius (pill variant)
- Use `componentRadius.tabsIndicator` for indicator radius
- Replace spring config `{ damping: 20, stiffness: 200 }` with `TABS_CONSTANTS.spring`

3. In TabsTrigger component:
- Replace `paddingVertical: spacing[3]` (underline) with `tabsTokens.triggerUnderline.paddingVertical`
- Replace `paddingVertical: spacing[2]` (pill) with `tabsTokens.trigger.paddingVertical`
- Replace `paddingHorizontal: spacing[4]` (underline) with `tabsTokens.triggerUnderline.paddingHorizontal`
- Replace `paddingHorizontal: spacing[3]` (pill) with `tabsTokens.trigger.paddingHorizontal`
- Replace hardcoded `fontWeight: '600'/'500'` with `tabsTokens.trigger.activeFontWeight`/`fontWeight`
- Replace hardcoded `fontSize: 14` in styles with `tabsTokens.trigger.fontSize`

4. In TabsContent component:
- Replace `marginTop: spacing[4]` with `tabsTokens.content.marginTop`

5. Update StyleSheet indicator styles:
- Replace `top: 4, bottom: 4` with token references
- Replace `height: 2` with `TABS_CONSTANTS.indicatorHeight`

Note: Access componentRadius via `useTheme()` hook which returns `componentRadius` in its return object.
  </action>
  <verify>
Run TypeScript compilation:
```bash
cd /Users/johanneschristler/Code/metacells/other/mcellui && npx tsc --noEmit -p packages/registry/tsconfig.json
```
Verify no errors. Check that tabs.tsx imports tabsTokens and TABS_CONSTANTS.
  </verify>
  <done>
Tabs component uses tabsTokens for all typography and spacing. Uses TABS_CONSTANTS for animation config. Uses componentRadius.tabs and componentRadius.tabsIndicator for border radii. No hardcoded font sizes or padding values remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Tabs functionality in demo app</name>
  <files>packages/registry/ui/tabs.tsx</files>
  <action>
After migration, verify the component still functions correctly:

1. Check that exports are intact (Tabs, TabsList, TabsTrigger, TabsContent)
2. Verify TypeScript types are preserved (TabsProps, TabsListProps, TabsTriggerProps, TabsContentProps)
3. Ensure no runtime-breaking changes (dynamic styles still use theme values correctly)

Review the component to confirm:
- `useTheme()` hook provides componentRadius
- Token references are using correct dot notation (tabsTokens.trigger.fontSize, not tabsTokens['trigger']['fontSize'])
- Spring config object is correctly spread or used
  </action>
  <verify>
Run demo app build check:
```bash
cd /Users/johanneschristler/Code/metacells/other/mcellui && npx tsc --noEmit -p apps/demo/tsconfig.json
```
Verify demo app compiles without errors referencing Tabs.
  </verify>
  <done>
Demo app compiles successfully. Tabs component exports remain unchanged. All TypeScript types are preserved. Token references use correct syntax.
  </done>
</task>

</tasks>

<verification>
1. tabs.tsx imports tabsTokens, TABS_CONSTANTS from @metacells/mcellui-core
2. TabsList uses componentRadius.tabs for pill container
3. TabsList uses TABS_CONSTANTS.spring for indicator animation
4. TabsTrigger uses tabsTokens.trigger.fontSize, fontWeight
5. TabsContent uses tabsTokens.content.marginTop
6. StyleSheet no longer contains hardcoded font sizes or padding
7. TypeScript compiles for both registry and demo packages
</verification>

<success_criteria>
- No hardcoded numeric values for fontSize, padding, or margin in tabs.tsx
- Indicator animation uses TABS_CONSTANTS.spring
- Container and indicator radii use componentRadius tokens
- Demo app compiles and renders Tabs correctly
</success_criteria>

<output>
After completion, create `.planning/phases/07-navigation-interaction/07-02-SUMMARY.md`
</output>

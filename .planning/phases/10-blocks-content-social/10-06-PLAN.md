# Plan 10-06: Social & Swipeable Blocks Demo Enhancement

```yaml
phase: 10-blocks-content-social
plan: 06
type: execute
wave: 3
depends_on:
  - 10-02
  - 10-03
  - 10-04
files_modified:
  - apps/demo/components/demos/blocks-demo.tsx
autonomous: true

must_haves:
  truths:
    - "FeedPostCard demo shows loading indicators on actions"
    - "CommentItem demo shows likeLoading state"
    - "CartItem demo shows swipe hint and quantityLoading"
  artifacts:
    - path: "apps/demo/components/demos/blocks-demo.tsx"
      provides: "Enhanced social and swipeable block demos"
      contains: "FeedPostCardPreview"
  key_links:
    - from: "apps/demo/components/demos/blocks-demo.tsx"
      to: "packages/registry/blocks/*.tsx"
      via: "import and render"
      pattern: "FeedPostCard|CommentItem|CartItem|UserListItem|ChatBubble"
```

<objective>
Enhance demo sections for social blocks (FeedPostCard, CommentItem, ChatBubble) and swipeable blocks (CartItem, UserListItem) to showcase loading states and interaction patterns.

Purpose: Demonstrate the enhanced social and swipeable block features from plans 10-03 and 10-04.
Output: blocks-demo.tsx with comprehensive social and swipeable block demos.
</objective>

<execution_context>
@/Users/johanneschristler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johanneschristler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-blocks-content-social/10-03-SUMMARY.md
@.planning/phases/10-blocks-content-social/10-04-SUMMARY.md
@apps/demo/components/demos/blocks-demo.tsx
</context>

<tasks>

<task type="auto">
  <name>Enhance FeedPostCard demo with loading states</name>
  <files>apps/demo/components/demos/blocks-demo.tsx</files>
  <action>
1. Read apps/demo/components/demos/blocks-demo.tsx
2. Locate FeedPostCardPreview function
3. Add comprehensive loading state demo:
   - Add useState for liked, likeCount, likeLoading, commentLoading
   - Create handleLike async function with 800ms delay
   - Create handleComment async function with 600ms delay
   - Show ActivityIndicator when loading instead of icon
   - Disable buttons during loading
4. Ensure ActivityIndicator is imported from react-native
  </action>
  <verify>
cd /Users/johanneschristler/Code/metacells/other/mcellui
grep -n "likeLoading\|commentLoading" apps/demo/components/demos/blocks-demo.tsx
grep -n "ActivityIndicator" apps/demo/components/demos/blocks-demo.tsx | head -5
npx tsc --noEmit -p apps/demo/tsconfig.json
  </verify>
  <done>
- FeedPostCard demo shows loading indicators on like/comment actions
- Buttons are disabled during loading
- State management demonstrates proper async handling
  </done>
</task>

<task type="auto">
  <name>Enhance CommentItem demo with loading states</name>
  <files>apps/demo/components/demos/blocks-demo.tsx</files>
  <action>
1. Locate CommentItemPreview function
2. Add loading state demonstration:
   - Add useState for liked, likeCount, likeLoading
   - Create handleLike async function with 500ms delay
   - Pass likeLoading prop to CommentItem
   - Add disabled nested reply example to show disabled state
3. Show both normal and disabled comment items
  </action>
  <verify>
cd /Users/johanneschristler/Code/metacells/other/mcellui
grep -n "likeLoading" apps/demo/components/demos/blocks-demo.tsx
grep -n "CommentItem" apps/demo/components/demos/blocks-demo.tsx -A 5 | head -20
npx tsc --noEmit -p apps/demo/tsconfig.json
  </verify>
  <done>
- CommentItem demo shows likeLoading state with ActivityIndicator
- Disabled nested reply demonstrates disabled state
- Both states are interactive and demonstrate enhanced props
  </done>
</task>

<task type="auto">
  <name>Enhance CartItem and UserListItem demos with interaction hints</name>
  <files>apps/demo/components/demos/blocks-demo.tsx</files>
  <action>
1. Locate CartItemPreview function
2. Add visual hint for swipe and loading state demo:
   - Add useState for quantity, quantityLoading
   - Create handleQuantityChange async function with 300ms delay
   - Pass quantityLoading to CartItem
   - Add hint text explaining swipe interaction
3. Locate UserListItemPreview function (or add if missing)
4. Ensure UserListItem demo shows:
   - Action button with loading state
   - Multiple users to show different states
5. Add styles if needed (variantLabel style if not present)
  </action>
  <verify>
cd /Users/johanneschristler/Code/metacells/other/mcellui
grep -n "quantityLoading" apps/demo/components/demos/blocks-demo.tsx
grep -n "Swipe left" apps/demo/components/demos/blocks-demo.tsx
grep -n "UserListItem" apps/demo/components/demos/blocks-demo.tsx | head -5
npx tsc --noEmit -p apps/demo/tsconfig.json
  </verify>
  <done>
- CartItem demo has hint text explaining swipe interaction
- CartItem demo shows quantityLoading state on Stepper
- UserListItem demo shows action button loading state
- All demos compile without TypeScript errors
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/johanneschristler/Code/metacells/other/mcellui

# Check loading state demos exist
grep -n "Loading\|setLoading" apps/demo/components/demos/blocks-demo.tsx | head -20

# Check swipe hint exists
grep -n "Swipe left" apps/demo/components/demos/blocks-demo.tsx

# Check ActivityIndicator usage
grep -n "ActivityIndicator" apps/demo/components/demos/blocks-demo.tsx | head -10

# Check social block demos
grep -n "FeedPostCardPreview\|CommentItemPreview\|CartItemPreview" apps/demo/components/demos/blocks-demo.tsx

# TypeScript compilation
npx tsc --noEmit -p apps/demo/tsconfig.json

# Run demo app to visually verify (optional manual check)
# cd apps/demo && npx expo start
```
</verification>

<success_criteria>
- [ ] FeedPostCard demo shows loading indicators on like/comment actions
- [ ] CommentItem demo shows likeLoading state with ActivityIndicator
- [ ] CommentItem demo shows disabled nested reply
- [ ] CartItem demo has hint text explaining swipe interaction
- [ ] CartItem demo shows quantityLoading state on Stepper
- [ ] UserListItem demo shows action button loading state
- [ ] ActivityIndicator is properly imported
- [ ] TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-blocks-content-social/10-06-SUMMARY.md`
</output>

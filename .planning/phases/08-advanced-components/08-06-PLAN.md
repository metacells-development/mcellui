---
phase: 08-advanced-components
plan: 06
type: execute
wave: 3
depends_on: ["08-02", "08-03", "08-04", "08-05"]
files_modified:
  - apps/demo/components/demos/calendar-demo.tsx
  - apps/demo/components/demos/datetime-picker-demo.tsx
  - apps/demo/components/demos/form-demo.tsx
  - apps/demo/components/demos/image-gallery-demo.tsx
  - apps/demo/components/demos/pagination-demo.tsx
  - apps/demo/components/demos/stories-demo.tsx
  - apps/demo/components/demos/search-input-demo.tsx
autonomous: true

must_haves:
  truths:
    - "All 7 advanced component demos show size variants (where applicable)"
    - "Demos show all interactive states (disabled, loading, error)"
    - "Demo structure follows Sizes -> Features -> States -> Use Cases pattern"
  artifacts:
    - path: "apps/demo/components/demos/calendar-demo.tsx"
      provides: "Calendar demo with sizes and modes"
    - path: "apps/demo/components/demos/search-input-demo.tsx"
      provides: "SearchInput demo with loading/clear states"
  key_links:
    - from: "apps/demo/components/demos/*-demo.tsx"
      to: "packages/registry/ui/*.tsx"
      via: "component imports"
      pattern: "import.*from.*registry"
---

<objective>
Enhance all 7 advanced component demos to show comprehensive variant and state coverage.

Purpose: Demonstrate all sizes, modes, and interactive states following the established demo pattern.

Output: All demos follow Sizes -> Features -> States -> Use Cases structure with complete coverage.
</objective>

<execution_context>
@/Users/johanneschristler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johanneschristler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-form-inputs/01-05-SUMMARY.md
@apps/demo/components/demos/Section.tsx
@apps/demo/components/demos/calendar-demo.tsx
@apps/demo/components/demos/datetime-picker-demo.tsx
@apps/demo/components/demos/form-demo.tsx
@apps/demo/components/demos/image-gallery-demo.tsx
@apps/demo/components/demos/pagination-demo.tsx
@apps/demo/components/demos/stories-demo.tsx
@apps/demo/components/demos/search-input-demo.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Calendar and DateTimePicker demos</name>
  <files>
    apps/demo/components/demos/calendar-demo.tsx
    apps/demo/components/demos/datetime-picker-demo.tsx
  </files>
  <action>
**Calendar Demo:**
Structure as:
1. **Sizes** - Show sm and md size variants side by side
2. **Selection Modes** - Single date, date range, multiple dates
3. **Features** - Week numbers, first day of week (Sunday/Monday)
4. **States** - Min/max date constraints, marked dates, disabled dates
5. **Use Cases** - Booking calendar with unavailable dates marked

Ensure each section uses the shared Section component:
```typescript
<Section title="Sizes">
  <View style={{ flexDirection: 'row', gap: 16 }}>
    <Calendar size="sm" value={date} onValueChange={setDate} />
    <Calendar size="md" value={date} onValueChange={setDate} />
  </View>
</Section>
```

**DateTimePicker Demo:**
Structure as:
1. **Modes** - Date, time, datetime
2. **States** - Default, disabled, error
3. **Features** - Min/max dates, 24-hour format, custom format
4. **Use Cases** - Appointment booking with time slots

Ensure disabled and error states are shown:
```typescript
<Section title="States">
  <DateTimePicker label="Disabled" value={date} onChange={setDate} disabled />
  <DateTimePicker label="Error" value={date} onChange={setDate} error="Date is required" />
</Section>
```
  </action>
  <verify>Demo app builds: `cd apps/demo && npx expo build --platform ios --no-wait`</verify>
  <done>Calendar demo shows sizes, modes, features, states. DateTimePicker demo shows modes, states, features.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance Form, ImageGallery, Pagination demos</name>
  <files>
    apps/demo/components/demos/form-demo.tsx
    apps/demo/components/demos/image-gallery-demo.tsx
    apps/demo/components/demos/pagination-demo.tsx
  </files>
  <action>
**Form Demo:**
Structure as:
1. **Basic Form** - Email + password with validation
2. **Form States** - Pristine, touched, error, submitting
3. **Field Types** - Input, Textarea, Select, Checkbox in a form
4. **Validation** - Zod schema validation with error messages
5. **Use Cases** - Contact form with required indicator

Show FormDescription and FormMessage:
```typescript
<FormItem>
  <FormLabel required>Email</FormLabel>
  <Input ... />
  <FormDescription>We'll never share your email.</FormDescription>
  <FormMessage />
</FormItem>
```

**ImageGallery Demo:**
Structure as:
1. **Grid Layouts** - 2, 3, 4 columns
2. **Aspect Ratios** - Square (1:1), landscape (16:9), portrait (3:4)
3. **Spacing** - Tight (2px), normal (4px), loose (8px)
4. **Features** - Custom border radius, disable viewer
5. **Use Cases** - Photo album, product images

**Pagination Demo:**
Structure as:
1. **Sizes** - sm, md, lg side by side
2. **Variants** - Dots, numbers, simple
3. **Features** - Show buttons, loop mode, max visible
4. **States** - First page (prev disabled), last page (next disabled)
  </action>
  <verify>Demo app builds: `cd apps/demo && npx expo build --platform ios --no-wait`</verify>
  <done>Form, ImageGallery, Pagination demos show complete variant and state coverage</done>
</task>

<task type="auto">
  <name>Task 3: Enhance Stories and SearchInput demos</name>
  <files>
    apps/demo/components/demos/stories-demo.tsx
    apps/demo/components/demos/search-input-demo.tsx
  </files>
  <action>
**Stories Demo:**
Structure as:
1. **Sizes** - sm, md, lg StoryAvatars
2. **States** - Unseen story (gradient ring), seen story (gray ring), no story
3. **Features** - Add story button, custom gradient colors, fallback initials
4. **Layout** - StoriesRow with multiple avatars
5. **Use Cases** - Social feed stories header

Show all ring states:
```typescript
<Section title="Ring States">
  <StoryAvatar source={...} name="Unseen" hasUnseenStory />
  <StoryAvatar source={...} name="Seen" seen />
  <StoryAvatar source={...} name="No Story" />
</Section>
```

**SearchInput Demo:**
Structure as:
1. **Basic** - Default search input
2. **States** - Default, focused, loading, with text (shows clear)
3. **Features** - Auto focus, custom placeholder, submit handler
4. **Controlled vs Uncontrolled** - Both patterns
5. **Use Cases** - Search header, filter list

Show loading state:
```typescript
<Section title="States">
  <SearchInput placeholder="Default" />
  <SearchInput value="Search term" loading />
  <SearchInput value="With clear" showClearButton />
</Section>
```
  </action>
  <verify>Demo app builds: `cd apps/demo && npx expo build --platform ios --no-wait`</verify>
  <done>Stories and SearchInput demos show sizes, states, features, and use cases</done>
</task>

</tasks>

<verification>
1. `cd apps/demo && npx expo build --platform ios --no-wait` succeeds (or `npx tsc --noEmit`)
2. All 7 demos follow Sizes -> Features -> States -> Use Cases structure
3. Calendar demo shows sm/md sizes
4. Pagination demo shows sm/md/lg sizes and all variants
5. SearchInput demo shows loading and clear button states
6. Form demo shows validation errors and FormMessage
</verification>

<success_criteria>
- All 7 demos enhanced with comprehensive coverage
- Demo structure consistent: Sizes -> Features -> States -> Use Cases
- All size variants shown where applicable
- All interactive states demonstrated (disabled, loading, error)
- Shared Section component used for organization
</success_criteria>

<output>
After completion, create `.planning/phases/08-advanced-components/08-06-SUMMARY.md`
</output>

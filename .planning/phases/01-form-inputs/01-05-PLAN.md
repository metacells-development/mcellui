---
phase: 01-form-inputs
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - apps/demo/components/demos/input-demo.tsx
  - apps/demo/components/demos/textarea-demo.tsx
  - apps/demo/components/demos/select-demo.tsx
  - apps/demo/components/demos/slider-demo.tsx
  - apps/demo/components/demos/stepper-demo.tsx
  - apps/demo/components/demos/checkbox-demo.tsx
  - apps/demo/components/demos/switch-demo.tsx
  - apps/demo/components/demos/radio-group-demo.tsx
  - apps/demo/components/demos/tag-input-demo.tsx
autonomous: true

must_haves:
  truths:
    - "Each demo shows all three sizes (sm, md, lg) with visually distinguishable proportions"
    - "Each demo shows disabled state (grayed out, non-interactive)"
    - "Each demo shows error state where applicable (red border/text visible)"
    - "All form inputs announce correctly with VoiceOver (iOS) and TalkBack (Android)"
    - "Focus state changes are visually verifiable by tapping each input"
  artifacts:
    - path: "apps/demo/components/demos/input-demo.tsx"
      provides: "Comprehensive Input demo with sizes, states"
      contains: "Sizes"
    - path: "apps/demo/components/demos/textarea-demo.tsx"
      provides: "Comprehensive Textarea demo with sizes, states"
      contains: "Sizes"
    - path: "apps/demo/components/demos/select-demo.tsx"
      provides: "Comprehensive Select demo with sizes, states"
      contains: "Sizes"
  key_links:
    - from: "apps/demo/components/demos/input-demo.tsx"
      to: "@/components/ui/input"
      via: "import"
      pattern: "size=.sm"
---

<objective>
Create comprehensive demo files for all 9 form input components showing sizes, states, and variants.

Purpose: Demos serve as visual documentation and testing ground. Each demo must show all variants, sizes (sm/md/lg), and states (default, disabled, error, focused) to verify the token migration worked correctly.

Output: 9 enhanced demo files following the ButtonDemo pattern with Section components.
</objective>

<execution_context>
@/Users/johanneschristler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johanneschristler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-form-inputs/01-RESEARCH.md
@.planning/phases/01-form-inputs/01-02-SUMMARY.md
@.planning/phases/01-form-inputs/01-03-SUMMARY.md
@.planning/phases/01-form-inputs/01-04-SUMMARY.md

# Reference files
@apps/demo/components/demos/button-demo.tsx (pattern reference)
@apps/demo/components/demos/input-demo.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Input, Textarea, Select demos</name>
  <files>
    apps/demo/components/demos/input-demo.tsx
    apps/demo/components/demos/textarea-demo.tsx
    apps/demo/components/demos/select-demo.tsx
  </files>
  <action>
Update demos to follow ButtonDemo pattern with comprehensive coverage.

**Input Demo (input-demo.tsx):**
```tsx
import React, { useState } from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { Input } from '@/components/ui/input';

export function InputDemo() {
  const [value, setValue] = useState('');

  return (
    <View style={styles.container}>
      <Section title="Sizes">
        <Input size="sm" label="Small" placeholder="Small input" />
        <Input size="md" label="Medium" placeholder="Medium input (default)" />
        <Input size="lg" label="Large" placeholder="Large input" />
      </Section>

      <Section title="With Features">
        <Input
          label="Email"
          placeholder="you@example.com"
          keyboardType="email-address"
          autoCapitalize="none"
        />
        <Input
          label="Password"
          placeholder="Enter password"
          secureTextEntry
          showPasswordToggle
        />
        <Input
          label="Clearable"
          placeholder="Type to clear"
          value={value}
          onChangeText={setValue}
          clearable
        />
        <Input
          label="With Helper"
          placeholder="Username"
          helperText="This will be your public display name"
        />
        <Input
          label="Character Count"
          placeholder="Bio"
          showCount
          maxLength={100}
        />
      </Section>

      <Section title="States">
        <Input
          label="Default"
          placeholder="Default state"
        />
        <Input
          label="With Error"
          placeholder="Email"
          value="invalid"
          error="Please enter a valid email"
        />
        <Input
          label="Disabled"
          placeholder="Can't edit"
          value="Read only"
          editable={false}
        />
      </Section>
    </View>
  );
}

function Section({ title, children }: { title: string; children: React.ReactNode }) {
  return (
    <View style={styles.section}>
      <Text style={styles.sectionTitle}>{title}</Text>
      <View style={styles.sectionContent}>{children}</View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { gap: 24 },
  section: { gap: 12 },
  sectionTitle: { fontSize: 14, fontWeight: '600', color: '#737373', textTransform: 'uppercase', letterSpacing: 0.5 },
  sectionContent: { gap: 12 },
});
```

**Textarea Demo (textarea-demo.tsx):**
```tsx
import React, { useState } from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { Textarea } from '@/components/ui/textarea';

export function TextareaDemo() {
  const [value, setValue] = useState('');

  return (
    <View style={styles.container}>
      <Section title="Sizes">
        <Textarea size="sm" label="Small" placeholder="Small textarea" rows={3} />
        <Textarea size="md" label="Medium" placeholder="Medium textarea (default)" rows={4} />
        <Textarea size="lg" label="Large" placeholder="Large textarea" rows={4} />
      </Section>

      <Section title="Auto Grow">
        <Textarea
          label="Auto-growing"
          placeholder="Start typing..."
          autoGrow
          minRows={2}
          maxRows={6}
          value={value}
          onChangeText={setValue}
        />
      </Section>

      <Section title="With Character Count">
        <Textarea
          label="Bio"
          placeholder="Tell us about yourself"
          showCount
          maxLength={200}
        />
      </Section>

      <Section title="States">
        <Textarea
          label="Default"
          placeholder="Default state"
          rows={3}
        />
        <Textarea
          label="With Error"
          placeholder="Description"
          error="Description is required"
          rows={3}
        />
        <Textarea
          label="Disabled"
          placeholder="Can't edit"
          value="Read only content"
          editable={false}
          rows={3}
        />
      </Section>
    </View>
  );
}

// ... Section component same as above
```

**Select Demo (select-demo.tsx):**
```tsx
import React, { useState } from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { Select } from '@/components/ui/select';

const countries = [
  { label: 'United States', value: 'us' },
  { label: 'Germany', value: 'de' },
  { label: 'Japan', value: 'jp' },
  { label: 'France', value: 'fr' },
  { label: 'United Kingdom', value: 'uk' },
];

export function SelectDemo() {
  const [value, setValue] = useState('');
  const [errorValue, setErrorValue] = useState('');

  return (
    <View style={styles.container}>
      <Section title="Sizes">
        <Select
          size="sm"
          label="Small"
          placeholder="Select country"
          options={countries}
          value={value}
          onValueChange={setValue}
        />
        <Select
          size="md"
          label="Medium"
          placeholder="Select country"
          options={countries}
          value={value}
          onValueChange={setValue}
        />
        <Select
          size="lg"
          label="Large"
          placeholder="Select country"
          options={countries}
          value={value}
          onValueChange={setValue}
        />
      </Section>

      <Section title="With Helper Text">
        <Select
          label="Country"
          placeholder="Select your country"
          options={countries}
          value={value}
          onValueChange={setValue}
          helperText="We use this for shipping calculations"
        />
      </Section>

      <Section title="States">
        <Select
          label="Default"
          placeholder="Select option"
          options={countries}
          value={value}
          onValueChange={setValue}
        />
        <Select
          label="With Error"
          placeholder="Select option"
          options={countries}
          value={errorValue}
          onValueChange={setErrorValue}
          error="Please select a country"
        />
        <Select
          label="Disabled"
          placeholder="Can't select"
          options={countries}
          disabled
        />
      </Section>
    </View>
  );
}

// ... Section component same as above
```
  </action>
  <verify>Demo app renders without errors when navigating to these components</verify>
  <done>Input, Textarea, Select demos show sizes (sm/md/lg), features, and states (default/error/disabled)</done>
</task>

<task type="auto">
  <name>Task 2: Enhance Slider, Stepper, Checkbox, Switch, Radio, TagInput demos</name>
  <files>
    apps/demo/components/demos/slider-demo.tsx
    apps/demo/components/demos/stepper-demo.tsx
    apps/demo/components/demos/checkbox-demo.tsx
    apps/demo/components/demos/switch-demo.tsx
    apps/demo/components/demos/radio-group-demo.tsx
    apps/demo/components/demos/tag-input-demo.tsx
  </files>
  <action>
Update remaining demos with consistent structure.

**All demos should include:**
1. Section for "Sizes" showing sm, md, lg
2. Section for "States" showing default, disabled, (error where applicable)
3. Section for component-specific variants/features

**Slider Demo:**
- Sizes: sm, md, lg with showValue
- Features: with label, with custom formatValue, step values
- States: default, disabled

**Stepper Demo:**
- Sizes: sm, md, lg
- Variants: default, outline, ghost
- States: default, disabled, at min/max (buttons become disabled)

**Checkbox Demo:**
- Sizes: sm, md, lg
- Features: with label, with description, indeterminate
- States: checked, unchecked, disabled

**Switch Demo:**
- Sizes: sm, md, lg
- Features: with label, with description
- States: on, off, disabled

**Radio Demo:**
- Sizes: sm, md, lg in separate RadioGroups
- Features: with description
- States: selected, unselected, disabled

**TagInput Demo:**
- Sizes: sm, md, lg
- Features: with suggestions, with maxTags, with validation
- States: default, with error, disabled

Each demo should:
1. Import Section component or define it locally
2. Use consistent styling (gap: 24 between sections, gap: 12 within sections)
3. Show realistic examples, not just "Test 1", "Test 2"
  </action>
  <verify>Demo app renders without errors when navigating to these components</verify>
  <done>All 6 demos show sizes (sm/md/lg) and states (default/disabled and component-specific states)</done>
</task>

</tasks>

<verification>
1. Run demo app: `cd apps/demo && npx expo start`
2. Navigate to each form input component demo
3. Verify each shows:
   - Size variants (sm, md, lg) with visible differences
   - Disabled state (grayed out, not interactive)
   - Error state where applicable (red border/text)
   - Focus state (tap input, see border color change)
4. Accessibility testing (required):
   - iOS: Enable VoiceOver (Settings > Accessibility > VoiceOver), navigate to each input
     - Verify: Input type announced (text field, checkbox, switch, slider, etc.)
     - Verify: Label announced correctly
     - Verify: State changes announced (checked/unchecked, on/off, value changes)
   - Android: Enable TalkBack (Settings > Accessibility > TalkBack), navigate to each input
     - Verify: Same announcements as iOS
     - Verify: Double-tap activates checkboxes/switches
</verification>

<success_criteria>
- All 9 form input demos have Sizes section with sm/md/lg
- All demos have States section with disabled state
- Input, Textarea, Select, TagInput demos show error state
- Demos follow consistent Section component pattern
- No TypeScript errors in demo files
- VoiceOver (iOS) and TalkBack (Android) correctly announce all inputs and state changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-form-inputs/01-05-SUMMARY.md`
</output>

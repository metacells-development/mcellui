---
phase: 12-screens
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - packages/registry/screens/profile-screen.tsx
  - packages/registry/screens/settings-screen.tsx
  - packages/registry/screens/account-screen.tsx
autonomous: true

must_haves:
  truths:
    - "Profile screen uses typography tokens for all text styling"
    - "Settings screen uses typography tokens for all text styling"
    - "Account screen uses typography tokens for all text styling"
    - "All three screens handle safe area insets correctly"
  artifacts:
    - path: "packages/registry/screens/profile-screen.tsx"
      provides: "Profile screen with theme tokens"
      contains: "fontSize"
    - path: "packages/registry/screens/settings-screen.tsx"
      provides: "Settings screen with theme tokens"
      contains: "fontSize"
    - path: "packages/registry/screens/account-screen.tsx"
      provides: "Account screen with theme tokens"
      contains: "fontSize"
  key_links:
    - from: "packages/registry/screens/profile-screen.tsx"
      to: "useTheme"
      via: "theme hook"
      pattern: "fontSize\\[|fontWeight\\."
---

<objective>
Migrate profile and settings-related screens (Profile, Settings, Account) to use theme typography tokens consistently.

Purpose: User management screens form a natural group with similar UI patterns - refining them together ensures consistent user experience.
Output: 3 screens migrated to full typography token usage.
</objective>

<execution_context>
@/Users/johanneschristler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johanneschristler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-screens/12-01-SUMMARY.md

@packages/registry/screens/profile-screen.tsx
@packages/registry/screens/settings-screen.tsx
@packages/registry/screens/account-screen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Profile screen to theme tokens</name>
  <files>packages/registry/screens/profile-screen.tsx</files>
  <action>
Migrate profile-screen.tsx to use typography tokens:

1. Expand useTheme destructuring: `const { colors, spacing, fontSize, fontWeight, lineHeight } = useTheme();`

2. Replace hardcoded font sizes in StyleSheet:
   - name (fontSize: 24) -> fontSize.xl (20) or fontSize['2xl'] (24)
   - username (fontSize: 15) -> fontSize.base (14)
   - bio (fontSize: 15, lineHeight: 22) -> fontSize.base, lineHeight.relaxed
   - statValue (fontSize: 20) -> fontSize.xl
   - statLabel (fontSize: 13) -> fontSize.xs (12)

3. Replace fontWeight values:
   - '700' -> fontWeight.bold
   - '600' -> fontWeight.semibold

4. Move font-related styles to inline dynamic styles while keeping layout styles in StyleSheet.

5. Profile screen already uses Avatar, Button, IconButton, Badge, Tabs - verify these are properly connected to theme.

Example transformation:
```typescript
// StyleSheet (keep layout only)
const styles = StyleSheet.create({
  name: {
    // Remove: fontSize: 24, fontWeight: '700'
  },
  // ...
});

// Inline (use tokens)
<Text style={[
  styles.name,
  {
    color: colors.foreground,
    fontSize: fontSize['2xl'],
    fontWeight: fontWeight.bold,
  }
]}>
  {user.name}
</Text>
```
  </action>
  <verify>
grep -c "fontSize: [0-9]" packages/registry/screens/profile-screen.tsx
# Should be 0 (no hardcoded numeric font sizes)
  </verify>
  <done>Profile screen uses typography tokens for all text styling</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Settings and Account screens to theme tokens</name>
  <files>packages/registry/screens/settings-screen.tsx, packages/registry/screens/account-screen.tsx</files>
  <action>
Migrate both settings-screen.tsx and account-screen.tsx to typography tokens:

For settings-screen.tsx:
1. Import typography tokens from useTheme
2. Replace hardcoded font sizes:
   - Section headers -> fontSize.lg (18)
   - Item labels -> fontSize.base (14)
   - Item descriptions -> fontSize.sm (13)
   - Version text -> fontSize.xs (12)
3. Replace fontWeight values with tokens

For account-screen.tsx:
1. Import typography tokens from useTheme
2. Replace hardcoded font sizes:
   - Screen title -> fontSize.lg (18)
   - Section titles -> fontSize.md (16)
   - Item labels -> fontSize.base (14)
   - Help text/descriptions -> fontSize.sm (13)
   - User name display -> fontSize.lg or fontSize.xl
   - User email -> fontSize.sm

3. Both screens likely use List component - verify it passes theme properly.

4. For both screens:
   - Keep layout styles (flex, alignItems, padding patterns) in StyleSheet
   - Move font-related styles to inline with tokens
   - Ensure all Text components use color tokens (colors.foreground, colors.foregroundMuted)
  </action>
  <verify>
grep -c "fontSize: [0-9]" packages/registry/screens/settings-screen.tsx
grep -c "fontSize: [0-9]" packages/registry/screens/account-screen.tsx
# Both should be 0
  </verify>
  <done>Settings and Account screens use typography tokens for all text styling</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. TypeScript check: `cd packages/registry && npx tsc --noEmit`
2. Verify no hardcoded font sizes:
   - `grep "fontSize: [0-9]" packages/registry/screens/profile-screen.tsx | wc -l`
   - `grep "fontSize: [0-9]" packages/registry/screens/settings-screen.tsx | wc -l`
   - `grep "fontSize: [0-9]" packages/registry/screens/account-screen.tsx | wc -l`
3. Verify token usage: `grep "fontSize\[" packages/registry/screens/profile-screen.tsx`
</verification>

<success_criteria>
- Profile screen has zero hardcoded numeric font sizes in StyleSheet
- Settings screen has zero hardcoded numeric font sizes in StyleSheet
- Account screen has zero hardcoded numeric font sizes in StyleSheet
- All three screens use fontWeight tokens instead of string literals
- All screens compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-screens/12-02-SUMMARY.md`
</output>

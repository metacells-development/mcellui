---
phase: 12-screens
plan: 04
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - packages/registry/screens/cart-screen.tsx
  - packages/registry/screens/checkout-screen.tsx
  - packages/registry/screens/product-detail-screen.tsx
  - packages/registry/screens/order-history-screen.tsx
autonomous: true

must_haves:
  truths:
    - "Cart screen uses typography tokens for all text styling"
    - "Checkout screen uses typography tokens for all text styling"
    - "Product Detail screen uses typography tokens for all text styling"
    - "Order History screen uses typography tokens for all text styling"
  artifacts:
    - path: "packages/registry/screens/cart-screen.tsx"
      provides: "Cart screen with theme tokens"
      contains: "fontSize"
    - path: "packages/registry/screens/checkout-screen.tsx"
      provides: "Checkout screen with theme tokens"
      contains: "fontSize"
  key_links:
    - from: "packages/registry/screens/cart-screen.tsx"
      to: "useTheme"
      via: "typography tokens"
      pattern: "fontSize\\[|fontWeight\\."
---

<objective>
Migrate e-commerce screens (Cart, Checkout, Product Detail, Order History) to use theme typography tokens consistently.

Purpose: E-commerce screens handle pricing, quantities, and order information - consistent typography ensures professional appearance and readability.
Output: 4 screens migrated to full typography token usage.
</objective>

<execution_context>
@/Users/johanneschristler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johanneschristler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-screens/12-01-SUMMARY.md

@packages/registry/screens/cart-screen.tsx
@packages/registry/screens/checkout-screen.tsx
@packages/registry/screens/product-detail-screen.tsx
@packages/registry/screens/order-history-screen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Cart and Checkout screens to theme tokens</name>
  <files>packages/registry/screens/cart-screen.tsx, packages/registry/screens/checkout-screen.tsx</files>
  <action>
Migrate cart-screen.tsx and checkout-screen.tsx to typography tokens:

For cart-screen.tsx:
1. Expand useTheme: `const { colors, spacing, radius, fontSize, fontWeight } = useTheme();`

2. Replace hardcoded font sizes:
   - headerTitle (fontSize: 18) -> fontSize.lg
   - summaryTitle (fontSize: 16) -> fontSize.md
   - summaryLabel (fontSize: 14) -> fontSize.sm
   - summaryValue (fontSize: 14) -> fontSize.sm
   - totalLabel (fontSize: 16) -> fontSize.md
   - totalValue (fontSize: 20) -> fontSize.xl
   - checkoutLabel (fontSize: 12) -> fontSize.xs
   - checkoutPrice (fontSize: 18) -> fontSize.lg

3. Replace fontWeight values:
   - '600' -> fontWeight.semibold
   - '700' -> fontWeight.bold
   - '500' -> fontWeight.medium

For checkout-screen.tsx:
1. Import typography tokens from useTheme

2. Replace hardcoded font sizes in both main styles and stepStyles:
   - headerTitle -> fontSize.lg
   - sectionTitle (fontSize: 18) -> fontSize.lg
   - stepStyles.circleText (fontSize: 12) -> fontSize.xs
   - stepStyles.label (fontSize: 11) -> fontSize.xs (closest token)
   - securityText (fontSize: 12) -> fontSize.xs
   - summaryTitle (fontSize: 14) -> fontSize.sm
   - summaryText (fontSize: 13) -> fontSize.xs
   - itemName/itemPrice (fontSize: 14) -> fontSize.sm
   - totalLabel/totalValue (fontSize: 14) -> fontSize.sm
   - grandTotalLabel (fontSize: 16) -> fontSize.md
   - grandTotalValue (fontSize: 18) -> fontSize.lg

3. Replace all fontWeight string values with tokens.

4. Move font-related styles to inline while keeping layout in StyleSheet.
  </action>
  <verify>
grep -c "fontSize: [0-9]" packages/registry/screens/cart-screen.tsx
grep -c "fontSize: [0-9]" packages/registry/screens/checkout-screen.tsx
# Both should be 0
  </verify>
  <done>Cart and Checkout screens use typography tokens</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Product Detail and Order History screens to theme tokens</name>
  <files>packages/registry/screens/product-detail-screen.tsx, packages/registry/screens/order-history-screen.tsx</files>
  <action>
Migrate product-detail-screen.tsx and order-history-screen.tsx to typography tokens:

For product-detail-screen.tsx:
1. Import typography tokens from useTheme

2. Replace hardcoded font sizes:
   - Product title -> fontSize.xl or fontSize['2xl']
   - Price display -> fontSize.xl, fontWeight.bold
   - Original price (strikethrough) -> fontSize.md
   - Description -> fontSize.base, lineHeight.relaxed
   - Section titles (Reviews, Specifications) -> fontSize.lg
   - Rating value -> fontSize.sm
   - Review count -> fontSize.xs
   - Specification labels -> fontSize.sm
   - Specification values -> fontSize.sm, fontWeight.medium
   - Add to cart button (handled by Button component)

3. Replace fontWeight values with tokens.

For order-history-screen.tsx:
1. Import typography tokens from useTheme

2. Replace hardcoded font sizes:
   - Header title -> fontSize.lg
   - Order number -> fontSize.base, fontWeight.medium
   - Order date -> fontSize.xs
   - Order status -> fontSize.xs, fontWeight.medium
   - Order total -> fontSize.md, fontWeight.semibold
   - Item names -> fontSize.sm
   - Empty state -> fontSize.xl (title), fontSize.base (message)

3. Replace fontWeight values with tokens.

4. Order status badges should use Badge component with proper variant.
  </action>
  <verify>
grep -c "fontSize: [0-9]" packages/registry/screens/product-detail-screen.tsx
grep -c "fontSize: [0-9]" packages/registry/screens/order-history-screen.tsx
# Both should be 0
  </verify>
  <done>Product Detail and Order History screens use typography tokens</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. TypeScript check: `cd packages/registry && npx tsc --noEmit`
2. Verify no hardcoded font sizes:
   ```bash
   for f in cart-screen checkout-screen product-detail-screen order-history-screen; do
     echo "$f: $(grep -c 'fontSize: [0-9]' packages/registry/screens/${f}.tsx 2>/dev/null || echo 0)"
   done
   ```
3. Verify token usage: `grep "fontSize\[" packages/registry/screens/checkout-screen.tsx`
</verification>

<success_criteria>
- Cart screen has zero hardcoded numeric font sizes
- Checkout screen has zero hardcoded numeric font sizes
- Product Detail screen has zero hardcoded numeric font sizes
- Order History screen has zero hardcoded numeric font sizes
- All screens use fontWeight tokens
- All screens compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-screens/12-04-SUMMARY.md`
</output>

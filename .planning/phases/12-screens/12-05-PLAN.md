---
phase: 12-screens
plan: 05
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - packages/registry/screens/home-screen.tsx
  - packages/registry/screens/search-screen.tsx
  - packages/registry/screens/followers-screen.tsx
  - packages/registry/screens/onboarding-screen.tsx
  - packages/registry/screens/help-screen.tsx
autonomous: true

must_haves:
  truths:
    - "Home screen uses typography tokens for all text styling"
    - "Search screen uses typography tokens for all text styling"
    - "Followers screen uses typography tokens for all text styling"
    - "Onboarding screen uses typography tokens for all text styling"
    - "Help screen uses typography tokens for all text styling"
  artifacts:
    - path: "packages/registry/screens/home-screen.tsx"
      provides: "Home screen with theme tokens"
      contains: "fontSize"
    - path: "packages/registry/screens/search-screen.tsx"
      provides: "Search screen with theme tokens"
      contains: "fontSize"
  key_links:
    - from: "packages/registry/screens/home-screen.tsx"
      to: "useTheme"
      via: "typography tokens"
      pattern: "fontSize\\[|fontWeight\\."
---

<objective>
Migrate content/utility screens (Home, Search, Followers, Onboarding, Help) to use theme typography tokens consistently.

Purpose: These screens represent the remaining screen types - dashboard, search, lists, onboarding flows, and support - completing full token coverage.
Output: 5 screens migrated to full typography token usage.
</objective>

<execution_context>
@/Users/johanneschristler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johanneschristler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-screens/12-01-SUMMARY.md

@packages/registry/screens/home-screen.tsx
@packages/registry/screens/search-screen.tsx
@packages/registry/screens/followers-screen.tsx
@packages/registry/screens/onboarding-screen.tsx
@packages/registry/screens/help-screen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Home and Search screens to theme tokens</name>
  <files>packages/registry/screens/home-screen.tsx, packages/registry/screens/search-screen.tsx</files>
  <action>
Migrate home-screen.tsx and search-screen.tsx to typography tokens:

For home-screen.tsx:
1. Expand useTheme: `const { colors, spacing, radius, fontSize, fontWeight, lineHeight } = useTheme();`

2. Replace hardcoded font sizes:
   - Welcome/greeting -> fontSize.xl or fontSize['2xl']
   - User name -> fontSize.lg, fontWeight.semibold
   - Section titles -> fontSize.lg
   - Stat values -> fontSize.xl, fontWeight.bold
   - Stat labels -> fontSize.xs
   - Card titles -> fontSize.md
   - Card descriptions -> fontSize.sm
   - "See all" links -> fontSize.sm, fontWeight.medium

3. Replace fontWeight values with tokens.

For search-screen.tsx:
1. Import typography tokens from useTheme

2. Replace hardcoded font sizes:
   - Search input (handled by SearchInput component)
   - Recent searches title -> fontSize.md
   - Recent search items -> fontSize.base
   - Search results title -> fontSize.lg
   - Result item title -> fontSize.base
   - Result item subtitle -> fontSize.sm
   - "No results" title -> fontSize.xl
   - "No results" message -> fontSize.base
   - Filter chips (handled by Chip component)

3. Replace fontWeight values with tokens.

4. Ensure SearchInput component passes through theme properly.
  </action>
  <verify>
grep -c "fontSize: [0-9]" packages/registry/screens/home-screen.tsx
grep -c "fontSize: [0-9]" packages/registry/screens/search-screen.tsx
# Both should be 0
  </verify>
  <done>Home and Search screens use typography tokens</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Followers, Onboarding, and Help screens to theme tokens</name>
  <files>packages/registry/screens/followers-screen.tsx, packages/registry/screens/onboarding-screen.tsx, packages/registry/screens/help-screen.tsx</files>
  <action>
Migrate followers-screen.tsx, onboarding-screen.tsx, and help-screen.tsx to typography tokens:

For followers-screen.tsx:
1. Import typography tokens from useTheme

2. Replace hardcoded font sizes:
   - Header title -> fontSize.lg
   - Tab labels (Followers/Following) -> fontSize.base
   - User name in list -> fontSize.base, fontWeight.medium
   - Username/handle -> fontSize.sm
   - Follow button (handled by Button component)
   - Empty state -> fontSize.xl (title), fontSize.base (message)

3. Replace fontWeight values with tokens.

For onboarding-screen.tsx:
1. Import typography tokens from useTheme

2. Replace hardcoded font sizes:
   - Slide title -> fontSize['2xl'] or fontSize.xl
   - Slide description -> fontSize.base, lineHeight.relaxed
   - Page indicator dots (no font)
   - Skip button -> fontSize.base
   - Next/Get Started button (handled by Button component)

3. Replace fontWeight values with tokens.

4. Ensure Carousel indicator styling uses radius tokens.

For help-screen.tsx:
1. Import typography tokens from useTheme

2. Replace hardcoded font sizes:
   - Header title -> fontSize.lg
   - Section titles (FAQ, Contact Us) -> fontSize.lg
   - FAQ question -> fontSize.base, fontWeight.medium
   - FAQ answer -> fontSize.sm, lineHeight.relaxed
   - Contact option title -> fontSize.base
   - Contact option subtitle -> fontSize.xs
   - Version info -> fontSize.xs

3. Replace fontWeight values with tokens.

4. FAQ items likely use Accordion - verify it passes theme.
  </action>
  <verify>
grep -c "fontSize: [0-9]" packages/registry/screens/followers-screen.tsx
grep -c "fontSize: [0-9]" packages/registry/screens/onboarding-screen.tsx
grep -c "fontSize: [0-9]" packages/registry/screens/help-screen.tsx
# All should be 0
  </verify>
  <done>Followers, Onboarding, and Help screens use typography tokens</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. TypeScript check: `cd packages/registry && npx tsc --noEmit`
2. Verify no hardcoded font sizes:
   ```bash
   for f in home-screen search-screen followers-screen onboarding-screen help-screen; do
     echo "$f: $(grep -c 'fontSize: [0-9]' packages/registry/screens/${f}.tsx 2>/dev/null || echo 0)"
   done
   ```
3. Verify token usage: `grep "fontSize\[" packages/registry/screens/home-screen.tsx`
</verification>

<success_criteria>
- Home screen has zero hardcoded numeric font sizes
- Search screen has zero hardcoded numeric font sizes
- Followers screen has zero hardcoded numeric font sizes
- Onboarding screen has zero hardcoded numeric font sizes
- Help screen has zero hardcoded numeric font sizes
- All screens use fontWeight tokens
- All screens compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-screens/12-05-SUMMARY.md`
</output>
